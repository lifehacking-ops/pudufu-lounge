# 프드프 디자인 시스템

`pudufu.net` 홈과 `/community`(라운지)를 2026-09-14 에 추출한 것. 렌더된 DOM 의
computed style 을 빈도순으로 집계했으므로, 선언된 의도가 아니라 **실제로 화면에 쓰인 값**이다.

> 한 줄 요약 — Tailwind 기본 그레이 스케일 위에 브랜드 블루 하나. 그림자는 거의 없고
> 면은 흰색, 구분은 1px 회색 선. 서체 하나로 10~22px 를 쓴다.

---

## 1. 컬러

### 브랜드

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--brand` | `#1F5AF2` | 주요 버튼, 활성 내비, 링크, 전송 버튼 |
| `--brand-tint` | `#EAF2FF` | 아바타 면, 카테고리 필, 활성 사이드바, 아이콘 타일 |

블루는 **하나뿐이고 진한 면으로는 거의 쓰지 않는다.** 홈 전체에서 `#1F5AF2` 를 배경으로
칠한 요소는 7개뿐이고, 나머지는 전부 `#EAF2FF` 연한 면에 블루 글자다.

### 중성 — Tailwind gray 를 그대로 쓴다

| 토큰 | 값 | Tailwind | 쓰임 |
|---|---|---|---|
| `--ink` | `#111827` | gray-900 | 제목·본문 |
| `--slate` | `#6B7280` | gray-500 | 보조 텍스트. **가장 많이 쓰인다** |
| `--mist` | `#9CA3AF` | gray-400 | 메타·플레이스홀더 |
| `--edge` | `#D1D5DB` | gray-300 | 버튼 테두리 |
| `--line` | `#E5E7EB` | gray-200 | 구분선 |
| `--hair` | `#F3F4F6` | gray-100 | 카드 테두리, 옅은 면 |
| `--canvas` | `#F5F5F7` | — | 페이지 배경 |
| `--surface` | `#FFFFFF` | — | 카드 면 |

### 의미색 — 연한 면 + 진한 글자 쌍

| 의미 | 글자 | 면 |
|---|---|---|
| 대기 · 지각 | `#D97706` | `#FFFBEB` |
| 완료 · 정시 | `#16A34A` | `#F0FDF4` |
| 할인 · 위험 | `#EF4444` | — |

의미색은 **항상 연한 면 + 같은 계열 진한 글자 쌍**으로 쓰고 테두리를 두르지 않는다.
이것이 이 시스템에서 가장 일관된 규칙이다.

---

## 2. 타이포그래피

- 서체 하나 : **Noto Sans KR**, 폴백 `-apple-system, system-ui, "Segoe UI"`
- 숫자 전용 서체 없음
- 자간 조정 없음 (`letter-spacing: normal`)

| 역할 | 크기 / 굵기 | 행간 |
|---|---|---|
| 페이지 타이틀 | 22 / 700 | 1.3 |
| 게시물 제목 | 18 / 700 | 1.4 |
| 섹션 제목 | 15 / 600 | 1.5 |
| 강조 본문 | 14 / 600 | 1.5 |
| 본문 | 14 / 400 | 1.5 |
| 보조 | 13 / 400 | 1.5 |
| 메타 | 12 / 400 | 1.4 |
| 배지 | 10~11 / 600 | 1.4 |

굵기는 400 · 500 · 600 · 700 네 단계뿐이다. 300 이하도, 800 이상도 쓰지 않는다.

---

## 3. 라운드

| 값 | 쓰임 |
|---|---|
| `8px` | 버튼, 입력, 작은 컨트롤 |
| `12px` | **게시물 카드** — 가장 많이 쓰는 카드 반경 |
| `16px` | 배너, 히어로 등 큰 면 |
| `9999px` | 배지, 필, 아바타 |
| `30px` | 하단 고정 입력 바 |

---

## 4. 그림자

거의 쓰지 않는다. 카드의 경계는 **1px `#F3F4F6` 테두리**가 맡고,
그림자는 진짜로 떠 있어야 하는 것에만 붙는다.

| 토큰 | 값 |
|---|---|
| sm | `0 1px 3px rgba(0,0,0,.06)` |
| md | `0 2px 6px rgba(0,0,0,.08)` |
| lg | `0 2px 12px rgba(0,0,0,.07)` |

---

## 5. 레이아웃

| 항목 | 값 |
|---|---|
| 컨테이너 | `1280px` |
| 카드 패딩 | `20px` |
| 카드 테두리 | `1px #F3F4F6` |

---

## 6. 컴포넌트

### 게시물 카드 — `/community`

흰 면 · 12px · 1px `#F3F4F6` · 패딩 20px

- **머리** 34px 원형 아바타(`#EAF2FF` 면 + 블루 글자) · 이름 14/600 · 카테고리 13/400 회색 ·
  상대 시각 · 우측 끝에 상태 배지
- **몸** 제목 18/700, 본문 14/400 `#6B7280`
- **발** 1px 구분선 뒤에 하트+수 · 눈+수, 우측 끝에 공유

### 배지

10/600 · `9999px` · 패딩 `2px 8px` · **테두리 없음**. 중립은 `#F3F4F6` 면에 `#6B7280` 글자.

### 버튼

| 종류 | 규칙 |
|---|---|
| 보조 | 흰 면, 1px `#D1D5DB`, 8px, 패딩 `6px 16px`, 14/400 |
| 주요 | `#1F5AF2` 면, 흰 글자, 8px |

### 입력

회색 면(`#F3F4F6`) 또는 흰 면 + 1px 테두리. 검색은 `9999px`, 하단 입력 바는 `30px`.

---

## 7. 가져오지 않은 것 — 현재 라운지의 정보구조

`/community` 는 좌측에 게시판 목록(라운지 홈 · 자유 커뮤니티 · 릴스 피드백 · 올인원 AI 피드백)을
두고, 글쓰기는 화면 하단 고정 바에 있다. 로그인 전에는 `로그인 후 글쓰기` 로 잠겨 있다.

이것은 노션 기획 7장이 **문제로 지목한 구조 그대로**다. 게시판을 오가며 화면이 전환되고,
글쓰기가 피드에서 분리되어 있다.

그래서 이 프로토타입은 **시각 언어만 가져오고 정보구조는 기획안을 따른다.**

| 축 | pudufu.net | 이 프로토타입 |
|---|---|---|
| 색 · 서체 · 라운드 · 그림자 | 그대로 가져옴 | 〃 |
| 게시판 사이드바 | 좌측에 4개 | 없음. 필터 칩으로 대체 |
| 글쓰기 위치 | 하단 고정 바 | 피드 최상단, 도착 즉시 |
| 라운지 범위 | 전 강의가 하나에 섞임 | 강의별로 분리, 상단 토글로 전환 |
